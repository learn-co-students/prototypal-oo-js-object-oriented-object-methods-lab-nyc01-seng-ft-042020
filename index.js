
// 1. Add cursors
    // option + click
    // command + option + up or down (have to disable Spectacle!)
// 2. Duplicate
    // shift + option + up or down
// 3. Select instances
    // command + d
    // command + shift + l
// 4. Global search & replace
    // command + shift + h to open
    // option + command + enter to submit
    // command + b to close
// 5. Cursor jumping
    // option + left or right
// 6. Selection
    // jump & select: option + shift + left or right
    // jump& select by line: function + shift + left, right, up, or down 
// 7. Move lines
    // option + arrow up or down
// 8. Easy format
    // whole file: option + shift + f
    // selected code: command K + command F
// 9. Toggle terminal
    // control + backtick
// 10. Open shortcuts
    // command K + command S


function beefMember(name, homeState, training) {
    this.name = name
    this.homeState = homeState
    this.training = training
    this.training = training
}

beefMember.prototype.veto = () => {
    return "No, I must disagree"
}
beefMember.prototype.veto = () => {
    return "No, I must disagree"
}
beefMember.prototype.veto = () => {
    return "No, I must disagree"
}
beefMember.prototype.veto = () => {
    return "No, I must disagree"
}

beefMember.prototype.approve = () => {
    return "You can do that!"
}

beefMember.prototype.doCharity = () => {
    return "I like to help people."
}

beefMember.prototype.releasePressStatement = () => {
    return "You will see great things from Scuber."
}

beefMember.prototype.sayHi = function () {
    return `Hi, my name is ${this.name}. I am from ${this.homeState}, and I was trained in ${this.training}.`
}

